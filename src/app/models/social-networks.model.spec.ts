import { SocialNetworksModel } from './social-networks.model';

describe('SocialNetworksModel', () => {
  it('should create an instance', () => {
    expect(new SocialNetworksModel()).toBeTruthy();
  });
});
